import Axios from 'axios'

const url = `${process.env.BASE_API_URL}/transfer`;

/*export const insertTransferencia = id =>
    Axios.post(url, { params: { accountId: id } }).then(response => {       
        return response.data;
    });*/